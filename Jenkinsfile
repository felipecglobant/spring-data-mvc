pipeline {

    agent any

    stages {
        stage('build') {
            steps {
                echo 'building the app'
            }
        }

        stage('test') {
            steps {
                echo 'testign the app'
            }
        }

        stage('deploy') {
            steps {
                echo 'deploying the app'
            }
        }
    }
}